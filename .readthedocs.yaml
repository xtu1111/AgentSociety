version: 2

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.11"

python:
  install:
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/conf.py
  fail_on_warning: false

# 多语言配置
# ReadTheDocs 会自动为每个语言创建单独的构建
# 中文作为默认语言
formats:
  - pdf
  - epub
